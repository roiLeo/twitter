{% extends "::base.html.twig" %}
    {% block title 'Tweet' %}

    {% block header %}
        <h1>Detail Tweet:</h1>
    {% endblock %}

{% block content %}
        <div class="panel panel-default">
            <div class="panel-heading">Postée le {{ tweet.createdAt|date('d/m/Y') }}</div>
            <div class="panel-body">
                {{ tweet.message }}
            </div>
        </div>
{% endblock %}

{% block footer %}
{{ parent() }}
<a href="{{ path('app_tweet_list') }}">LIEN VERS LA PAGE TWEET</a>
{% endblock %}
